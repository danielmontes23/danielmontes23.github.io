<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title> Daniel's Website </title>
    
    <link rel="stylesheet" href="../styles/style.css">

    <script src="../components/time.js" defer></script>
    <script src="../components/date.js" defer></script>

</head>
<body class="h-screen flex items-center justify-center">

        <!-- Time -->
         
        <time class="container mx-auto block grid lg:grid-cols-12 md:grid-cols-2 font-mono" style="font-family: Arial;">
            <data class="hours text-9x1">HH</data> :
            <data class="minutes text-9x1">MM</data> :
            <data class="seconds text-9x1">SS</data>
            <data class="amOrPM text-9x1">AM/PM</data>
        </time>
    
        <!-- Date -->
        <time class="date font-mono" style="font-family: Arial;">
        <data class="dayOfWeek text-9x1">SMTWTFS</data> -
        <data class="month text-9x1">Month</data>
        <data class="dayOfMonth text-9x1">00</data>,
        <data class="year text-9x1">0000</data> 
        </time>


        <nav style="text-align: center; font-family: Arial, sans-serif;">
            <ul style="display: inline-block; list-style-type: none; padding: 0; margin-right: 20px;">
            <li><button class="btn"><a href="/about" style="text-decoration: none;">About Me</a></button></li>
            </ul>
            <ul style="display: inline-block; list-style-type: none; padding: 0;">
            <li><button class="btn"><a href="/projects" style="text-decoration: none;">Projects</a></button></li>
            </ul>
        </nav>

        <div class="">
            <marquee behavior="" direction="" style="font-size: 4rem; font-family: Arial;">WELCOME TO MI SITIO WEB COMPAS</marquee>
        </div>
        <img src="profilepic.jpg" alt="My profile pic" style="display: block; margin-left: auto; margin-right: auto;">
        
        <style>
            body{
                background: #C8102E;
            }
            time {
                text-align: center;
                font-size: 2rem;
                display: block;
                margin: 0 auto;
            }
        </style>
</body>
        <script>
            function updateTime() {
                const now = new Date();
                const hours = now.getHours();
                const minutes = now.getMinutes();
                const seconds = now.getSeconds();
                const amOrPM = hours >= 12 ? 'PM' : 'AM';

                document.querySelector('.hours').textContent = String(hours % 12 || 12).padStart(2, '0');
                document.querySelector('.minutes').textContent = String(minutes).padStart(2, '0');
                document.querySelector('.seconds').textContent = String(seconds).padStart(2, '0');
                document.querySelector('.amOrPM').textContent = amOrPM;

                const daysOfWeek = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];
                const months = [
                    'January', 'February', 'March', 'April', 'May', 'June',
                    'July', 'August', 'September', 'October', 'November', 'December'
                ];

                document.querySelector('.dayOfWeek').textContent = daysOfWeek[now.getDay()];
                document.querySelector('.month').textContent = months[now.getMonth()];
                document.querySelector('.dayOfMonth').textContent = String(now.getDate()).padStart(2, '0');
                document.querySelector('.year').textContent = String(now.getFullYear());
            }

            setInterval(updateTime, 1000);
            updateTime();
        </script>
</html>