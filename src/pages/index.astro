<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Daniel's Website </title>
    
    <link rel="stylesheet" href="../styles/style.css">

    <script src="../components/time.js" defer></script>
    <script src="../components/date.js" defer></script>

</head>
<body class="h-screen flex items-center justify-center">

        <!-- Time and Date Container -->
        <div class="time-date-container">
            <!-- Buttons -->
            <nav class="nav">
                <ul class="nav-list">
                    <li><button class="btn"><a href="/" class="nav-link">Home</a></button></li>
                <li><button class="btn"><a href="/about" class="nav-link">About Me</a></button></li>
                <li><button class="btn"><a href="/projects" class="nav-link">Projects</a></button></li>
                <li><button class="btn"><a href="/contact" class="nav-link">Contact</a></button></li>
                </ul>
            </nav>

            <!-- Time -->
            <time class="time">
            <data class="hours">HH</data> :
            <data class="minutes">MM</data> :
            <data class="seconds">SS</data>
            <data class="amOrPM">AM/PM</data>
            </time>
            
            <!-- Date -->
            <time class="date">
            <data class="dayOfWeek">SMTWTFS</data> -
            <data class="month">Month</data>
            <data class="dayOfMonth">00</data>,
            <data class="year">0000</data> 
            </time>
        </div>

        <div class="welcome-message">
            <marquee behavior="" direction="">Hello I am Daniel, Welcome to my Website!</marquee>
        </div>
        <!-- <img src="profilepic.jpg" alt="My profile pic" class="profile-pic"> -->

        <div class="projects" style="margin-top: 600px;">
            <h2><u>Projects in CSCD 378</u></h2>
            <ul class="projects-list">
            <li><a href="https://danielmontes23-landingpage.netlify.app/" target="_blank">Landing Page I have created.</a></li>
            <li><a href="https://date-time-tailwind-dmontes2.vercel.app/" target="_blank">Website which shows the current Date and Time.</a></li>
            <li><a href="https://danielmontes23-landingpage.netlify.app/" target="_blank">Landing Page I have created.</a></li>
            </ul>
        </div>
    
        <style>
            body {
            background: url('https://goeags.com/images/2023/5/18/Generic_Logo_campus_bg.png?width=1416&height=797&mode=crop&quality=80&format=jpg') no-repeat center center fixed;
            background-size: cover;
            }
            time {
            text-align: center;
            font-size: 2rem;
            display: block;
            margin: 0 auto;
            }
            .nav {
            text-align: center;
            font-family: Arial, sans-serif;
            }
            .nav-list {
            display: inline-block;
            list-style-type: none;
            padding: 0;
            margin: 0;
            }
            .nav-list li {
            display: inline;
            margin: 0 10px;
            }
            .nav-link {
            text-decoration: none;
            }
            .welcome-message {
            font-size: 4rem;
            font-family: Arial;
            color: white;
            text-align: center;
            }
            .profile-pic {
            display: block;
            margin-left: auto;
            margin-right: auto;
            }
            .time-date-container {
            font-family: Arial, sans-serif;
            color: white;
            text-align: center;
            }
            .time, .date {
            display: block;
            margin: 0 auto;
            font-size: 2rem;
            }
            .time data, .date data {
            font-size: 2rem;
            }
            h2{
            text-align: center;
            }
            .projects {
            text-align: center;
            color: white;
            }
            .projects-list {
            display: flex;
            justify-content: space-around;
            list-style-type: none;
            padding: 0;
            margin: 0;
            }
            .projects-list li {
            margin: 0 10px;
            }
            .projects-list a {
            text-decoration: none;
            font-size: 1.2rem;
            color: white;
            }
            .projects-list a:hover {
            text-decoration: underline;
            }
        </style>
    </body>
        <script>
            function updateTime() {
            const now = new Date();
            const hours = now.getHours();
            const minutes = now.getMinutes();
            const seconds = now.getSeconds();
            const amOrPM = hours >= 12 ? 'PM' : 'AM';

            document.querySelector('.hours').textContent = String(hours % 12 || 12).padStart(2, '0');
            document.querySelector('.minutes').textContent = String(minutes).padStart(2, '0');
            document.querySelector('.seconds').textContent = String(seconds).padStart(2, '0');
            document.querySelector('.amOrPM').textContent = amOrPM;

            const daysOfWeek = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];
            const months = [
                'January', 'February', 'March', 'April', 'May', 'June',
                'July', 'August', 'September', 'October', 'November', 'December'
            ];

            document.querySelector('.dayOfWeek').textContent = daysOfWeek[now.getDay()];
            document.querySelector('.month').textContent = months[now.getMonth()];
            document.querySelector('.dayOfMonth').textContent = String(now.getDate()).padStart(2, '0');
            document.querySelector('.year').textContent = String(now.getFullYear());
            }

            setInterval(updateTime, 1000);
            updateTime();
        </script>
    </html>